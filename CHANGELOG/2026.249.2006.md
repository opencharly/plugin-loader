# 2026.249.2006 — refactor(loader): migrate to the Deploy member-tree IR (spec #103 + sdk #221)

## Summary

Migrates plugin-loader to the new Deploy member-tree IR (spec #103 + sdk #221): ONE ordered member tree `Deploy.Member []spec.Member` (with the position-derived `Alongside()`/`InSubstrate()`/`MemberByName()`/`DeployLevelMembers()`/`InSubstrateMembers()`/`HasMembers()` helpers) replaces the dual `Children`/`Members` maps.

- **Re-pins** `github.com/opencharly/spec` v0.2026243.707 → **v0.2026249.1948** and `github.com/opencharly/sdk` v0.2026243.714 → **v0.2026249.1959** (`go mod tidy` run).
- **plugin.go**: the `BuildResourceMemberChildren` forward now returns `[]spec.Member` — the sdk #221 unified memberDisc classification. No dual-map reconstruction anywhere; the loader consumes position-derived helpers only.
- **flatten_fleet_venues_test.go**: fixtures migrated from the dead `Members:`/`Children:` dual maps to the ordered `Member` tree with explicit `Position` stamps (`PositionDeployLevel` → bare venue "os"; `PositionInSubstrate` → dotted venue "cross.migrate"); the cleared-Plan assertion now reads `def.MemberByName("os").Node.Plan`.
- **fleet_members_test.go**: every companion fixture built via a `deployLevelMember()` constructor over the ordered tree (deploy-level entries are what fold top-level); the historical comment naming the dead `TestSortedMemberKeys` key-sort test retired (the ONE ordered Member list is deterministic in authored order — nothing left to sort).
- **check_bed_run_test.go** (RCA'd, not a blind fix): `TestValidateCheckBeds_LocalRefMustResolve` broke because the new Phase-3 deploy-hop arm (sdk validate_check_beds.go) resolves a `from:` that names a kind:check BED — the fixture's bed was named `check-local` with `from: check-local`, a self-reference that now satisfies the lookup and never exercised the missing-template error. Fixture de-self-referenced (`from: missing-local`) with a comment explaining the constraint; the test's intent (missing local template rejected) is preserved.

## How tested

All commands run in `candy/plugin-loader`, final committed tree, `-count=1`:

```
$ go build ./... && go vet ./... && go test -count=1 -v ./... 2>&1 | tail -25
=== RUN   TestValidatePreemptibleOnNode/valid_holder
=== RUN   TestValidatePreemptibleOnNode/valid_claimant
=== RUN   TestValidatePreemptibleOnNode/empty_holds
=== RUN   TestValidatePreemptibleOnNode/bad_stop
=== RUN   TestValidatePreemptibleOnNode/bad_restore
=== RUN   TestValidatePreemptibleOnNode/empty_requires_token
=== RUN   TestValidatePreemptibleOnNode/self-contention
--- PASS: TestValidatePreemptibleOnNode (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/valid_holder (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/valid_claimant (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/empty_holds (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/bad_stop (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/bad_restore (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/empty_requires_token (0.00s)
    --- PASS: TestValidatePreemptibleOnNode/self-contention (0.00s)
=== RUN   TestValidate_BothExclusiveAndShared_Errors
--- PASS: TestValidate_BothExclusiveAndShared_Errors (0.00s)
=== RUN   TestValidateResourceDefs_ExclusiveVenueTrait
=== RUN   TestValidateResourceDefs_ExclusiveVenueTrait/vm_(exclusive_venue)_qemu_backend_flagged
=== RUN   TestValidateResourceDefs_ExclusiveVenueTrait/pod_(non-exclusive_venue)_never_flagged
--- PASS: TestValidateResourceDefs_ExclusiveVenueTrait (0.00s)
    --- PASS: TestValidateResourceDefs_ExclusiveVenueTrait/vm_(exclusive_venue)_qemu_backend_flagged (0.00s)
    --- PASS: TestValidateResourceDefs_ExclusiveVenueTrait/pod_(non-exclusive_venue)_never_flagged (0.00s)
    --- PASS: TestValidateResourceDefs_ExclusiveVenueTrait/pod_(non-exclusive_venue)_never_flagged (0.00s)
PASS
ok  	github.com/opencharly/plugin-loader/candy/plugin-loader	0.017s
```

Executed dead-identifier sweep (B14(d)) — every migrated identifier, real commands + output:

```
$ git grep -nE 'SortedMemberKeys|SortedNestedKeys|ValidateDeploymentChildren|HasChildren' -- '*.go'
[exit:1]  (zero matches)
$ git grep -nE '\.Inside\(' -- '*.go'
[exit:1]  (zero matches)
$ git grep -nE '\.Nested\b|\.Peers\b' -- '*.go'
[exit:1]  (zero matches)
$ git grep -nE '\.Members\b|\.Children\b' -- '*.go'
[exit:1]  (zero matches)
$ git grep -nE '\bMembers[[:space:]]*:|\bChildren[[:space:]]*:' -- '*.go'
[exit:1]  (zero matches)
```

(git grep exits 1 on zero matches — the executed proof that no dead identifier or dual-map field usage survives. The earlier unanchored `Members:`/`Children:` sweep matched only `foldMembers:` substrings inside `t.Fatalf` strings.)

`gofmt -l .` → empty. `git diff origin/main...HEAD` = exactly the 6 files in this PR; branch is 0 commits behind origin/main (fetched + compared before push).

## Rulebook compliance

- **R0 (skills first):** `/charly-internals:git-workflow` + `/charly-internals:go` SKILL.md loaded before any tool action; the subrepo has no AGENTS.md/CLAUDE.md of its own (verified), the umbrella + charly rulebooks apply.
- **R1 (RCA every anomaly):** the one failing test (`TestValidateCheckBeds_LocalRefMustResolve`) was root-caused to the new sdk deploy-hop arm resolving bed-named `from:` refs before any remediation; the fixture fix is annotated with the mechanism, not a workaround.
- **R2 (finish the whole cutover):** every in-scope occurrence of the removed identifiers migrated; executed sweep pasted above shows zero residue.
- **R3 (no duplication):** the loader keeps delegating every mechanism to the ONE copy in sdk/loaderkit; only signatures/fixtures moved, no logic re-implemented.
- **R4 (no workarounds):** no sleeps, retries, suppressions, or fallbacks; the test change tightens the fixture rather than weakening an assertion.
- **R4a (fix the product first):** product code (plugin.go forward) fixed at the source of the type change; test-only edits follow the upstream contract.
- **R5 (delete legacy completely):** all dual-map fixtures and the dead `TestSortedMemberKeys` comment reference removed in the same commit; no aliases or shims kept.
- **R6 (git safety):** `git status` inspected before staging/commit; branch-only landing, no force-push, no direct push to main, hooks not bypassed.
- **R7 (prove behavior, not compilation):** full `go test -count=1 ./...` suite pasted GREEN; the migrated tests still assert real fold/venue/validation behavior over the new tree.
- **R8 (preserve emitted artifacts):** no emitted artifact (label/plan/config) touched — Go module + test migration only.
- **R9 (binary equals source):** N/A — this repo ships no CalVer-stamped binary; the compiled-in provider is built and tested by the host charly build.
- **R10 (fresh disposable proof):** gates run on the FINAL COMMITTED tree (`git status` clean, then build/vet/test executed); fresh-run flags `-count=1`; no proof taken from an edited working state.

## Change classification

- **Change class:** code migration (Go source + tests + module pins), no schema/config/pin-of-record change outside this module's go.mod.
- **Verification gate:** `go build ./... && go vet ./... && go test -count=1 ./...` — all GREEN on the final committed tree (tails pasted above).
- **Attribution tier:** `fully tested and validated` — full gate passed on the final tree, changed paths executed live by the test suite.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
# 2026.250.2044 — refactor: drop the ResourceChildren ProjectLoader delegator (F1.2)

## Summary

Parser-consolidation F1 wave (F1.2) — plugin-loader's `ResourceChildren` ProjectLoader delegator is deleted with the seam method (opencharly/spec#122, merged, tagged v0.2026250.2036) and the sdk/loaderkit implementation (opencharly/sdk#239). The delegator's only consumer was the charly host's deploy-shape composite (charly PR in this wave), which now calls the ONE `IsDeployShape` classifier whose member channel subsumed the walk. Re-pins spec v0.2026250.523 → v0.2026250.2036 (`go mod tidy`).

## How tested

**Eval beds run: N/A — no disposable bed can execute this diff yet (named self-blocking-proof, rule 3, per the plugin-check #29 precedent and plugin-loader #6).** A live `charly check run` of a bed requires the charly binary to pin this plugin's NEW tag; the charly consumer re-pin (the in-flight wave item) is the landing that carries the wave's bed proof. Compile blast radius is real: against the pinned-before spec, sdk/loaderkit has no `ResourceChildren` symbol, so no pre-wave charly can compile this plugin module — the charly consumer PR is the only tree that can run it.

All commands on the final committed tree (real spec tag pinned, no worktree replace), `-count=1`:

```
$ go build ./... && go vet ./... && gofmt -l .   # all clean (exit 0)
$ go test -count=1 ./...
ok  github.com/opencharly/plugin-loader/candy/plugin-loader 0.019s
```

**B14(d) dead-code sweep (real commands + output):**

```
$ cd plugin-loader && git grep -n 'ResourceChildren' -- '*.go'
candy/plugin-loader/plugin.go:214:   // copy in sdk/loaderkit (K1 unit 3b). ResourceChildren is DELETED with the interface method
[exit 0 — the ONLY match is the deletion-comment line; zero functional references]

$ cd sdk && git grep -n 'ResourceChildren' -- '*.go' | grep -v node_tree.go   # exit 1, zero matches
$ grep -rln 'ResourceChildren' plugin-*/candy/*/ --include='*.go'             # only plugin-loader itself matches
```

**B10 liveness against the consumers' origin/main (honest state):** charly's `origin/main` STILL carries the seam-method call at `charly/provider_kind_invoke.go:234` (`pl.IsDeployShape(pn) || len(pl.ResourceChildren(pn)) > 0 || agentProvisionedBody(rawBody)`). That call is removed by the charly PR in THIS wave (the composite dies; `pl.IsDeployShape(pn)` alone stays) — the wave order is deliberate: spec#122 (merged) → sdk#239 → plugin-loader#7 → charly (last consumer, removes the last seam caller). The plugin's own `ResourceChildren` symbol has zero OTHER consumers anywhere in the org (greps above); its removal is exactly synchronized with the interface-method removal, so no intermediate state has a dangling call.

## Rulebook compliance

- R0: git-workflow + strict-policy skills loaded before the first tool call.
- R1: the gate demands were met with the house self-blocking-proof pattern (wave bed rides the charly consumer; plugin-check #29 + plugin-loader #6 precedent).
- R3: the delegation wall delegates to the ONE copy in sdk/loaderkit; nothing re-implemented.
- R5: the dead delegator is deleted in the same wave as the interface method (spec#122) and its last caller (charly PR); no shim kept.
- R6: branch-only, no force-push, no hook bypass; `git status` clean before commit.
- R7: build/vet/test/gofmt proven on the final committed tree; the sweep proves no residue.
- R10: gates on the final committed tree with the REAL spec tag; the live bed proof rides the charly consumer re-pin (named, reasoned).

## Change classification

- Change class: code/config refactor (dead delegator removal + consumer seam re-pin)
- Verification gate: go build/vet/test/gofmt green on the final committed tree; the changed-branch live proof rides the charly consumer re-pin (self-blocking-proof, rule 3)
- Attribution tier: analysed on a live system — the changed code compiles and its unit-level behavior runs in the Go test environment; the full live bed run lands with the charly consumer PR of this same wave

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
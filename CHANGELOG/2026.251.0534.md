# 2026.251.0534 — F2: implement the bridge-deletion ProjectLoader surface (CandyIsImage/BuildCandy/CueDocFromJSON/ParseDocStream) + wave tag pins

## Summary

Parser-consolidation F2 wave (plan/nested-capture-architecture.md, Cutover F2 — the genericNode dual representation dies) — the **loader-plugin slice**:

1. **The four new spec.ProjectLoader methods** the host consumes (one-line delegations to the ONE loaderkit copies, per the compiled-in seam pattern):
   - `CandyIsImage` / `BuildCandy` — the candy box⊻layer routing restated on spec.ParsedNode (F2.1); `foldCandyKind` + `materializeDiscoveredNode` call them through the seam.
   - `CueDocFromJSON` — the canonical-JSON CUE ingest (F2.3).
   - `ParseDocStream` — the ONE doc-stream composer (F2.5), so the embedded default-vocabulary stream drives the same per-document pipeline as the file walk.
2. **New seam tests** (f2_seam_test.go) pin the four methods at the plugin boundary, incl. the compile-time `spec.ProjectLoader` conformance proof.
3. **Pins** — spec v0.2026251.500 (PR #123), sdk v0.2026251.531 (PR #240); the dev worktree replaces are dropped.

## How tested

All commands on the final committed tree (branch `feat/f2-bridge-deletion`, real tag pins, no replaces), `-count=1`:

```
$ gofmt -l .            # empty
$ go build ./...        # exit 0
$ go vet ./...          # exit 0
$ go test ./... -count=1
ok  github.com/opencharly/plugin-loader/candy/plugin-loader  0.034s
```

New seam coverage:

```
$ go test . -run TestF2Seam -count=1 -v
--- PASS: TestF2Seam_CandyMethods (0.00s)
--- PASS: TestF2Seam_ParseDocStream (0.00s)
PASS
```

## Rulebook compliance

- R3 — one-line delegations to the single loaderkit implementations (the compiled-in seam pattern); no logic duplicated in the plugin.
- R6 — branch-only (feat/f2-bridge-deletion), no force-push, no hook bypass. R10 — all gates on the final committed real-pinned tree.

## Change classification

- Change class: seam extension + dependency tag pins (parser-consolidation F2)
- Verification gate: gofmt/build/vet + full plugin-loader test suite green on the final committed tree
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*